<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.voice">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UploadRecordsAct" android:label="Upload"></activity>
        <activity android:name=".PlayingAct" android:label="Playing"/>
        <activity android:name=".ReplaceAct" android:label="Replace"/>
        <activity android:name=".RecordInfoAct" android:label="Record Information"/>
        <activity android:name=".ListRecordsAct" />
        <activity android:name=".ListLabelsAct" android:label="List Labels"/>
        <activity android:name=".RecordingAct" android:label="Recording"/>
        <activity android:name=".RecordManagerAct" android:label="Record Manager"/>
        <activity android:name=".RegisAct" android:label="Add Email"/>
        <activity android:name=".MainActivity" android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>